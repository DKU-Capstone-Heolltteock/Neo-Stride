<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="200dp"
    android:layout_height="wrap_content"
    app:cardBackgroundColor="#1A1A1A"
    app:cardCornerRadius="20dp"
    app:cardElevation="15dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingVertical="12dp">

        <LinearLayout android:id="@+id/menu_account" style="@style/ProfileMenuItem">
            <ImageView android:src="@drawable/ic_account" style="@style/ProfileMenuIcon" app:tint="#CCFF00" />
            <TextView android:text="계정관리" style="@style/ProfileMenuText" />
        </LinearLayout>

        <LinearLayout android:id="@+id/menu_settings" style="@style/ProfileMenuItem">
            <ImageView android:src="@drawable/ic_settings" style="@style/ProfileMenuIcon" app:tint="#CCFF00" />
            <TextView android:text="앱 설정" style="@style/ProfileMenuText" />
        </LinearLayout>

        <LinearLayout android:id="@+id/menu_help" style="@style/ProfileMenuItem">
            <ImageView android:src="@drawable/ic_help" style="@style/ProfileMenuIcon" app:tint="#CCFF00" />
            <TextView android:text="고객센터" style="@style/ProfileMenuText" />
        </LinearLayout>

        <LinearLayout android:id="@+id/menu_logout" style="@style/ProfileMenuItem">
            <ImageView android:src="@drawable/ic_logout" style="@style/ProfileMenuIcon" app:tint="#FF4444" />
            <TextView android:text="로그아웃" style="@style/ProfileMenuText" />
        </LinearLayout>

        <TextView
            android:id="@+id/label_community"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginHorizontal="12dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="4dp"
            android:background="@drawable/shape_menu_border"
            android:gravity="center_vertical"
            android:paddingStart="15dp"
            android:text="커뮤니티"
            android:textColor="#CCFF00"
            android:textSize="13sp"
            android:textStyle="bold"
            android:clickable="false"
            android:focusable="false" />

        <LinearLayout android:id="@+id/menu_mypage" style="@style/ProfileMenuItem">
            <ImageView android:src="@drawable/ic_mypage" style="@style/ProfileMenuIcon"/>
            <TextView android:text="마이페이지" style="@style/ProfileMenuText" />
        </LinearLayout>

    </LinearLayout>
</androidx.cardview.widget.CardView>