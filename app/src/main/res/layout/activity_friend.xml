<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="30dp"
    android:background="#121212">

    <!-- 상단 헤더 영역 -->
    <LinearLayout
        android:id="@+id/layout_header"
        android:layout_width="match_parent"
        android:layout_height="60dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        app:layout_constraintTop_toTopOf="parent">

        <ImageButton
            android:id="@+id/btn_back"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_arrow_left"
            app:tint="#CCFF00" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:gravity="center"
            android:text="친구"
            android:textColor="#CCFF00"
            android:textSize="20sp"
            android:textStyle="bold" />

        <View android:layout_width="40dp" android:layout_height="40dp" />
    </LinearLayout>

    <!-- 4개 탭 버튼 영역 -->
    <HorizontalScrollView
        android:id="@+id/scroll_tabs"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingVertical="12dp"
        android:scrollbars="none"
        app:layout_constraintTop_toBottomOf="@id/layout_header">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp">

            <!-- 친구목록: ic_friend_list 연결 -->
            <TextView
                android:id="@+id/tab_friend_list"
                style="@style/FriendTabStyle"
                android:background="@drawable/bg_badge_btn"
                android:drawableStart="@drawable/ic_friend_list"
                android:drawablePadding="6dp"
                android:text="친구목록"
                android:textColor="#000000" />

            <!-- 보낸 요청: ic_friend_sent_requests 연결 -->
            <TextView
                android:id="@+id/tab_sent_requests"
                style="@style/FriendTabStyle"
                android:layout_marginStart="8dp"
                android:drawableStart="@drawable/ic_friend_sent_request"
                android:drawablePadding="6dp"
                android:text="보낸 요청" />

            <!-- 받은 요청: ic_friend_requests 연결 -->
            <FrameLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp">

                <TextView
                    android:id="@+id/tab_received_requests"
                    style="@style/FriendTabStyle"
                    android:drawableStart="@drawable/ic_friend_received_request"
                    android:drawablePadding="6dp"
                    android:text="받은 요청" />

                <View
                    android:id="@+id/badge_received_requests"
                    android:layout_width="8dp"
                    android:layout_height="8dp"
                    android:layout_gravity="top|start"
                    android:layout_marginStart="2dp"
                    android:layout_marginTop="2dp"
                    android:background="@drawable/bg_plan_dot"
                    android:backgroundTint="#FF3B30"
                    android:visibility="gone" />

            </FrameLayout>

            <!-- 차단목록: ic_friend_blocked 연결 -->
            <TextView
                android:id="@+id/tab_blocked_list"
                style="@style/FriendTabStyle"
                android:layout_marginStart="8dp"
                android:drawableStart="@drawable/ic_friend_blocked"
                android:drawablePadding="6dp"
                android:text="차단목록" />
        </LinearLayout>
    </HorizontalScrollView>

    <!-- 친구 리스트 RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_friends"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/scroll_tabs"
        tools:listitem="@layout/item_friend" />

</androidx.constraintlayout.widget.ConstraintLayout>